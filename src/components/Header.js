//Header that can be reused on all pages; includes the company name and logo
const Header = () => {
    return (
        <header>
            PLACEHOLDER
        </header>
    )
}

export default Header
